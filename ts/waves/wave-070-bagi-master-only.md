Wave 070 — bagi-master-only

**Propagate**
Remote `main` removed; `master` force-updated from former `main` commits; local branch renamed to `master` tracking `origin/master`; docs/README + GITHUB-BAGI use **master** only.

**Relax / Tension detected**
Merge with old `origin/master` had unrelated histories + conflicts — aborted; single-branch fix via `main:master` force.

**Break**
Dropped duplicate remote branch **main**.

**Evolve**
One branch: **`master`** on **BAGI**.

**Final stable configuration / Answer**
`git push origin master` — no separate `main`.
