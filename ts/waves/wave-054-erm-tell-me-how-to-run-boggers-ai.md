Wave 054 — erm-tell-me-how-to-run-boggers-ai

**Propagate**
User asked how to run BoggersTheAI; activation: README install/first-run, CLI `boggers`, dashboard `dashboard-start`, optional Ollama.

**Relax / Tension detected**
None — documentation already complete; this wave is instructional only.

**Break**
N/A.

**Evolve**
Stable answer: cd to repo → pip install → ollama optional → boggers / dashboard-start.

**Final stable configuration / Answer**
See parent response: install from `BoggersTheAI/`, `pip install -e ".[llm]"` or `".[all]"`, run `ollama serve` if using LLM, then `boggers` or `dashboard-start` on http://localhost:8000.

Official: https://boggersthefish.com/ · https://github.com/BoggersTheFish/BoggersTheAI
