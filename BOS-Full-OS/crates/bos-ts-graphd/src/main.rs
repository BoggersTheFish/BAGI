//! **TS** = Thinking System / Thinking Wave — **not** TypeScript.
//! Userspace graph daemon stub: inode ↔ `TsNodeState`, IPC for `bos-ts-orbital` (see `ts/vfs-graph.toml`).
//! Keep in sync with `recipes/bos-ts-graphd/source/src/main.rs`.

fn main() {
    println!("bos-ts-graphd — UniversalLivingGraph / VFS stub (IPC + redoxfs TBD)");
    println!("Contract: ts/vfs-graph.toml — see docs/VFS-UNIVERSAL-GRAPH.md");
}
