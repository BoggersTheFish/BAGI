//! **TS** = Thinking System / Thinking Wave — **not** TypeScript.
//! Update channel stub: signed manifests + rollback attach to `ts/update-channel.toml` (roadmap Phase 3).
//! Keep in sync with `recipes/bos-ts-updater/source/src/main.rs`.

fn main() {
    println!("bos-ts-updater — TS update channel (stub; fetch + verify TBD)");
    println!("Manifest: ts/update-channel.toml (version nodes → edges in later waves).");
}
