//! **TS** = Thinking System / Thinking Wave — **not** TypeScript.
//! Installer wave stub: disk layout + reconciliation will attach here (see docs/TS-DESKTOP-CLASS-ROADMAP.md).
//! Keep in sync with `recipes/bos-ts-installer/source/src/main.rs`.

fn main() {
    println!("bos-ts-installer — TS installer wave (stub; upstream Redox installer TBD)");
    println!("Phase 2: constraint graph over partitions → future waves.");
}
