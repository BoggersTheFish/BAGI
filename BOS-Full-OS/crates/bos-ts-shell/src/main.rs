//! **TS** = Thinking System / Thinking Wave — **not** TypeScript.
//! For web-only TypeScript: <https://www.typescriptlang.org/docs/>
//!
//! Redox: target `x86_64-unknown-redox`. This binary is a **stub** until Orbital
//! canvas + IPC to read `TensionRing` from kernel are wired (see README).

fn main() {
    // Placeholder: on Redox, link to orbital/libredox for window + draw graph.
    // For host `cargo check`, keep stdout.
    println!("bos-ts-shell — TS portal (build for Redox target)");
}
